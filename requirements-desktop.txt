# Minimal dependencies for desktop app only
paddlepaddle>=2.5.0
paddleocr>=2.7.0
PyMuPDF>=1.23.0
numpy>=1.21.0
PySide6>=6.6.0

# paddlex 3.x hidden dependencies — not declared in its pip metadata,
# but imported at runtime. Must be explicitly listed so CI installs them
# and PyInstaller can bundle them.
openpyxl>=3.1.0      # paddlex → tablepyxl → openpyxl
lxml>=4.9.0          # paddlex → tablepyxl → lxml
premailer>=3.10.0    # paddlex → tablepyxl → premailer
jinja2>=3.0.0        # paddlex → tokenizer_utils → jinja2
regex                # paddlex → text recognition
beautifulsoup4       # paddlex → html parsing (bs4)
einops               # paddlex → model inference
ftfy                 # paddlex → text processing
imagesize            # paddlex → image utilities
safetensors          # paddlex → model loading
scikit-learn         # paddlex → sklearn
sentencepiece        # paddlex → tokenizer
tiktoken             # paddlex → tokenizer
tokenizers           # paddlex → tokenizer
rapidfuzz            # paddlex → text matching
pyclipper            # paddlex → polygon clipping
shapely              # paddlex → geometry
scipy                # paddlex → scientific computing
PyYAML               # paddlex → config parsing
cssutils             # premailer dependency
cssselect            # lxml dependency
cachetools           # premailer dependency
